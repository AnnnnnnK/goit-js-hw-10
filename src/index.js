import { fetchBreeds, fetchCatByBreed } from "/src/cat-api";

fetchBreeds()
// .then((breeds) => {
//   const markup = breeds.map(({ id, name }) => `<option value="${id}">${name}</option>`);
//   select.innerHTML = markup;
// })

// axios.defaults.headers.common["x-api-key"] = "live_V85TSSsGlPdM0g6OiuHucrzbUsTFhTuodM7JeCGIdl3NV5F5IMxg03OCh5PxmxeK";
// console.log(axios.defaults.headers.common);

// fetch()